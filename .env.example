# Copy this file to .env and fill in real values.
# NEVER commit the actual .env file to git — add it to .gitignore.

DB_HOST=127.0.0.1
DB_NAME=xrentals
DB_USER=root
DB_PASS=
DB_PORT=3306

# Set to "production" on the live server. Turns off verbose PHP errors.
APP_ENV=development
